// Register theme-specific icon overrides here.
// Import a theme pack and add it to this map when a new UI theme is created.

import { PIXEL_THEME_ICONS } from './pixel.js';

export const THEME_ICON_OVERRIDES = Object.freeze({
  pixel: PIXEL_THEME_ICONS,
});
